# Lệnh generate migration

```bash
npx typeorm migration:generate -d src/data-source.ts src/migrations/MigrationName
```

# Lệnh run migration

```bash
npx typeorm migration:run -d src/data-source.ts
```
